// Filename: audio.js
define([
  'underscore',
  'backbone'
],
function(_, Backbone) {

    var audio = Backbone.Model.extend({

        initialize: function() {
            //console.log('Item initialized.');
        }

    });

    return audio;
});
