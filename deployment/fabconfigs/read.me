copy the baseconfig.json as newconf.json and leave
only the keys you want to override there.

for configs that you want to exclude from entering git
use underscore in front of the filename. e.g. "_newconf.json". 

