// Filename: button.js
define([
  'underscore',
  'backbone'
],
function(_, Backbone) {

    var button = Backbone.Model.extend({

        initialize: function() {
            //console.log('Item initialized.');
        }

    });

    return button;
});
